dependencies {
    implementation project(':common')

    implementation group: 'io.undertow', name: 'undertow-servlet', version: undertow_version
}

application {
    mainClass = "io.kineticedge.ks101.${project.name}.Main"
}
